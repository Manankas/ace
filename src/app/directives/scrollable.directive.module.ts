import { NgModule } from '@angular/core';

import { ScrollableDirective } from './scrollable.directive';

@NgModule({
  imports: [],
  declarations: [ScrollableDirective],
  exports: [ScrollableDirective],
})
export class ScrollableDirectiveModule {}
